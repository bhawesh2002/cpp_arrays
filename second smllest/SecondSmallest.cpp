#include <iostream>

using namespace std;

int main()
{
    int num[5] = {342, 24, 556, 23, 857};
    int a = sizeof(num) / sizeof(num[0]);
    for (int i = 0; i < a; i++)
    {
        /* code */
    }

    return 0;
}